'use client';

import AlertModalContainer from '@/shared/ui/modal/AlertModalContainer';
import ModalContainer from '@/shared/ui/modal/ModalContainer';

export default function PortalContainer() {
  return (
    <>
      <AlertModalContainer />
      <ModalContainer />
    </>
  );
}
